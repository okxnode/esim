# esim-ltd 源码包说明

**打包时间**：2026-09-24
**版本**：v1 雏形（可构建、可部署）

---

## 包含文件

```
esim-ltd/
├── hugo.toml                    站点配置（含 PlansJSON 自定义输出）
├── Makefile                     build / serve / test / audit / check
├── README.md                    部署指南
├── .gitignore
├── content/                     26 篇内容
│   ├── destinations/            10 个目的地页
│   ├── providers/               6 个品牌评测页
│   ├── guide/                   4 篇教程
│   ├── compare/index.md         比价工具页
│   ├── deals/index.md
│   └── about.md
├── data/plans.json              ★ 唯一资费数据源（6 品牌 / 27 套餐 / 10 目的地）
├── layouts/                     模板
│   ├── _default/                baseof / single / list / tool（比价工具专用）
│   ├── destinations/single.html
│   ├── partials/                header / footer
│   ├── index.html               首页
│   └── index.plansjson.json     构建时输出 /plans.json
├── assets/
│   ├── css/main.css             单文件样式（无 Tailwind）
│   └── js/compare.js            比价组件（纯函数层 + 渲染层）
├── scripts/
│   ├── test_compare.py          29 项测试
│   ├── set_affiliate.py         联盟链接接入 / 撤销
│   └── audit_affiliate.py       联盟链接体检
└── docs/
    └── 联盟链接接入指南.md       申请顺序 + 合规红线
```

**已排除**（构建产物与缓存，解压后 `hugo` 会自动重新生成）：
`public/`、`resources/_gen/`、`.hugo_build.lock`、`__pycache__/`、`*.pyc`

---

## 快速开始

需要 **Hugo extended 0.140+**、python3。（不需要 node / npm）

```bash
tar -xzf esim-ltd-src-20260924.tar.gz
cd esim-ltd

# 装 Hugo（如果没有）
HUGO_VERSION=0.140.2
curl -sL -o /tmp/hugo.tar.gz \
  "https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
tar xzf /tmp/hugo.tar.gz -C /tmp && mkdir -p ~/.local/bin
cp /tmp/hugo ~/.local/bin/hugo && chmod +x ~/.local/bin/hugo
hugo version    # 确认输出含 "extended"

export PATH="$HOME/.local/bin:$PATH"
make check      # 构建 + 29 项测试 + 联盟体检
make serve      # http://localhost:1313
```

---

## 已验证

本包已做过**独立解压 → 构建 → 测试**的完整验证：

| 项目 | 结果 |
|---|---|
| 独立构建 | 34 页 / 121ms / 零错误零警告 |
| 测试 | 29 / 29 通过 |
| 联盟体检 | 全部通过 |
| 构建产物 | `/plans.json` 正常生成 |

---

## 部署到 Cloudflare Pages

1. 推送到 GitHub
2. Cloudflare Pages → 连接仓库
3. **构建命令**：`hugo --gc --minify`
4. **输出目录**：`public`
5. ⚠️ **环境变量必须设 `HUGO_VERSION=0.140.2`** —— 不设会用 Pages 默认的旧版本，构建直接失败

---

## 上线前待补

- [ ] **隐私政策页** `content/privacy.md`（模板见 `docs/联盟链接接入指南.md` 第四节）—— 联盟平台审核会查
- [ ] 申请联盟并接入链接（`python3 scripts/set_affiliate.py <品牌id> <链接>`）
- [ ] 填写联系邮箱（页脚 / about / 隐私政策）
- [ ] 可选：Cloudflare Analytics token（`hugo.toml` 里 `cfAnalyticsToken`）

---

## 当前状态

- 联盟链接：**0/6 已接入**（都指向品牌官网，`make aff-list` 可查）
- 域名：`esim.ltd`（`hugo.toml` 里 baseURL 已配好）
- 内容：26 篇；规划书中另列出 15 个待写选题
